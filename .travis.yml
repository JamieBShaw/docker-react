language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jamiebshaw/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jamiebshaw/docker-react npm run test
